# PAUGERAN Server

Backend Axum, SQLite, Case State Machine, Reasoning Mode, dan Case Graph. `cargo build --release --bin paugeran` membangun frontend terlebih dahulu melalui `build.rs` lalu menghasilkan binary tunggal.
